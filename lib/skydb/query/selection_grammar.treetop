grammar SelectionGrammar
  rule root
    fields <SkyDB::Query::Ast::SelectionSyntaxNode>
  end

  rule fields
    field (',' field)*
  end

  rule field
    identifier <SkyDB::Query::Ast::SelectionFieldSyntaxNode>
  end

  rule identifier
    ([a-zA-Z_] [a-zA-Z0-9_]*) 
  end
end
